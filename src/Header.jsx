const Header = () => {
    // Since this is on more than one line, it must be placed in parens.
    return(
        <header>
            <h1>Header > H1.</h1>
        </header>
    )
}

export default Header
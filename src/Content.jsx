const Content = () => {
    // Since this is on more than one line, it must be placed in parens.
    return(
        <main>
            <h1>Content > H1.</h1>
        </main>
    )
}

export default Content
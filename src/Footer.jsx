const Footer = () => {
    // Since this is on more than one line, it must be placed in parens.
    return(
        <footer>
            <h1>Footer > H1.</h1>
        </footer>
    )
}

export default Footer